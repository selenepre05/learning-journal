#include <stdio.h>
int main() {
    int age = 30;
    if (age >= 18) {
        printf("You are old enough to vote!\n");
    } else {
        printf("You are not yet old enough to vote.\n");
    }
    return 0;
}
